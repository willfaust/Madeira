# Reference repos (not part of the project, just for research)
box64/
LiveContainer/
MeloNX/
# research/ holds upstream clones for study, except research/dxmt which is
# an active fork submodule — list siblings explicitly.
research/angle/
research/attesor/
research/box64/
research/dxvk/
research/FEX/
research/gamescope/
research/LiveContainer/
research/MoltenVK/
research/ProjectChampollion/
research/Proton/
research/StikDebug/
research/vkd3d-proton/
research/Winlator-Ludashi/
research/*.md

# Build artifacts
app/build/
app/DerivedData/
build/DerivedData/
build/xcode-derived/
FEX/build-ios/
build/ntdll-unix/obj/
build/wineserver/obj/

# Compiled libraries (rebuilt from build scripts)
app/Madeira/libntdll_unix.a
app/Madeira/libwineserver.a
app/libwineserver.a
app/Madeira/libdxmt_combined.a
app/Madeira/libwin32u_unix.a
app/libwin32u_unix.a

# win32u-unix build artifacts
build/win32u-unix/obj/

# DXMT build artifacts
build/dxmt-ios/obj/
build/dxmt-ios/shader-headers/
build/dxmt-ios/libdxmt_unix.a
build/dxmt-ios/libdxmt_combined.a
# The aarch64-windows PE tree, built by build-pe.sh on macOS and by the same
# sources with a Linux mingw + wine tree (see AGENTS.md).
build/dxmt-ios/pe-linux/

# D3D11 triangle test — compiled DXBC shaders + host-side HLSL compiler
build/d3d11-triangle/vs.dxbc
build/d3d11-triangle/ps.dxbc
build/d3d11-triangle/vs_dxbc.h
build/d3d11-triangle/ps_dxbc.h
build/d3d11-triangle/hlsl_compile.exe
build/d3d11-triangle/triangle.exe

# DXMT test-suite per-test build output
build/dxmt-tests/out/

# Wineboot subprocesses — dormant while Phase 3+ uses a pre-seeded prefix
# and skips wineboot. Rebuilt from the wine submodule if needed (requires
# bison 3.x + llvm-dlltool). services.exe + rpcss.exe graduated to tracked
# with the 2026-07-07 rpcss milestone (SCM + COM are live now).
app/Madeira/aarch64-windows/plugplay.exe
app/Madeira/aarch64-windows/svchost.exe
app/Madeira/aarch64-windows/winedevice.exe

# Toolchains (pulled by build scripts)
toolchains/

# Xcode
*.xcuserdata
*.xcworkspace
xcuserdata/

# macOS
.DS_Store
*.swp
*~

# Console/crash logs (large, device-specific)
console-log.txt
crash-log.txt
crash-log,txt
stikdebug-log.txt

# Superseded build dirs
wine-ios-build/

# wine/ is now a submodule — don't ignore it

# StikDebug scripts (reference only)
MeloNX.js
build/gnutls-ios/src/*
!build/gnutls-ios/src/*.tar.*
!build/gnutls-ios/src/SHA256SUMS

build/gnutls-ios/obj/

# Reference/scratch material — not build inputs.
# research/* rather than research/, so specific subdirectories can be
# re-included; git will not descend into a directory ignored with a trailing
# slash, which makes any negation inside it silently ineffective.
research/*
!research/remote-metal/
*.bak
photorec.se2
.DS_Store

# Build/deploy artifacts (regenerable; keep them out of history)
*.ipa
clean-prefix/
build/x64-tests/asmconf_gen/
build/x64-tests/asmconf_obj/

# Microsoft Visual C++ runtime redistributables.
# Microsoft-authored binaries, redistributable only under the Visual Studio
# redistributable terms and only unmodified. Not ours to ship -- obtain them
# yourself, see tools/fetch-vcruntime.md.
app/Madeira/x86_64-vcruntime/*.dll
research/remote-metal/host/rmetald
research/remote-metal/guest/rmtest

# remote-metal test binaries (built by run-tests.sh)
research/remote-metal/guest/rmtest
research/remote-metal/guest/rmreplay
research/remote-metal/guest/rmres
research/remote-metal/guest/rmclient_test
research/remote-metal/host/rmetald

# Python bytecode from importing the tools/ modules directly.
__pycache__/
*.pyc
